package calculadora;

public class Calculadora {
	//public static void main(String[] args) {
	//Lleva el método realizado en el ejercicio anterior a una nueva clase llamada “Calculadora”. 
		//public static Integer sumar (Integer numero, Integer numero2) {
		//	return numero+numero2;
		public static Integer sumar(Integer numero, Integer numero2) {
			Integer suma = numero + numero2;
			//y que devuelva el resultado de la suma.
			return suma;
		}
		
		/*public static Integer restar (Integer numero, Integer numero2) {
			return numero-numero2;
		}
		public static Integer multiplicar (Integer numero, Integer numero2) {
			return numero*numero2;
		}
		public static Integer dividir (Integer numero, Integer numero2) {
			return numero/numero2;
		}
		public static Integer calcular(Integer numero, Integer numero2) {
			Integer resultado = 0;
			System.out.println(resultado);
			String cadena;
			if (cadena.equalsIgnoreCase("sumar"));{
			resultado = sumar(numero,numero2);	
		}
			else   (cadena.equalsIgnoreCase("restar")); 
		    resultado = restar(numero,numero2);
	    }
			else  (cadena.equalsIgnoreCase("multiplicar")); 
	        resultado = multiplicar(numero,numero2); 
        }
			else (cadena.equalsIgnoreCase("dividir")); 
            resultado = dividir(numero,numero2);
        }
			else (cadena.equalsIgnoreCase("calcular")); 
			resultado = calcular(numero,numero2);
        }*/
    
}

 
	
